// CustomViewButton.jsx
import React from 'react';

const CustomViewButton = ({ onClick }) => (
  <button onClick={onClick} className="custom-view-button">
    View
  </button>
);

export default CustomViewButton;
